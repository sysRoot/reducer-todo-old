{
    "version": 2,
    "name": "todolist",
    "builds": [{ "src": "todolist/package.json", "use": "@now/static-build", "config": {"distDir": "build"} }],
    "routes": [
        { "src": "/(.*)", "dest": "todolist/$1" }
    ]
}